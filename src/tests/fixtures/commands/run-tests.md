---
description: Run tests with optional filtering
---

Run the test suite for the project.

Arguments: $ARGUMENTS

If specific tests are requested, filter by: $1

Use file: @package.json to check test configuration.

Run: !`bun test` to execute tests.
